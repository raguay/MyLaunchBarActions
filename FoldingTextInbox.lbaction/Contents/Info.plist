<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <key>CFBundleIdentifier</key>
    <string>com.customct.launchbar.FoldingText.Inbox</string>

    <key>CFBundleName</key>
    <string>FT-Inbox</string>

    <!-- <key>CFBundleShortVersionString</key> -->
    <!-- <value>1.0</value> -->

    <key>CFBundleVersion</key>
    <string>1.0</string>

    <key>CFBundleIconFile</key>
    <string>icon</string>

    <key>LBDebugLogEnabled</key>
    <false />

    <!-- LB stuff -->
    <!-- <key>LSMinimumSystemVersion</key> -->
    <!-- <string>10.9.1</string> -->

    <!-- <key>LBMinimumLaunchBarVersion</key> -->
    <!-- <string>6011</string> -->

    <!-- Per-action keys -->
    <key>LBTextInputTitle</key>
    <string>ft:</string>

    <key>LBScripts</key>
    <dict>
        <key>LBDefaultScript</key>
        <dict>
            <key>LBLiveFeedbackEnabled</key>
            <true/>
            <key>LBRequiresArgument</key>
            <true/>
            <key>LBReturnsResult</key>
            <true/>
            <key>LBRunInBackground</key>
            <false/>
            <key>LBScriptName</key>
            <string>default.js</string>
        </dict>
    </dict>

    <key>LBDescription</key>
    <dict>
        <key>LBAuthor</key>
        <string>Richard Guay</string>

        <key>LBWebsite</key>
        <string>http://customct.com</string>

        <key>LBEmail</key>
        <string>raguay@customct.com</string>

        <key>LBTwitter</key>
        <string>@CustomComputerT</string>

        <!-- A short summary about what the action does and how it’s used. -->
        <key>LBSummary</key>
        <string>Many useful actions with FoldingText.</string>

        <!-- Describe the arguments that should be passed to the action. For example, if
             your action’s default script specifies that it accepts paths via
             LBAcceptedArgumentTypes, but then the script itself checks that the user
             passed in a PDF file, this would be the right place to document that. -->
        <key>LBArgument</key>
        <string></string>

        <!-- Describe the action’s result. For example, an action fetching news via RSS
             that then shows a list of items representing the single stories in the RSS
             feed could document here that the resulting items represent the news
             articles. -->
        <key>LBResult</key>
        <string>Perform an action with the FoldingText program.</string>

        <!-- Describe any further requirements for your action to work, e.g. if it
             requires an active internet connection, iTunes to be playing a song, or
             a certain version of an app to be installed. -->
        <!-- <key>LBRequirements</key> -->
        <!-- <string></string> -->
    </dict>

</dict>
</plist>
